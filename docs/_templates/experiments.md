# 実験インデックス

> 全実験の俯瞰表。各実験の詳細は `experiments/expXXX.md`。定式化の全体像は `formulations.md`。

## 現在のフォーカス

（次にやること 1 つ。セッション終了時に必ず更新する）

- 現在のフェーズ: Phase ? / 未達ゲート:
- 直近 2 実験の定式化: F? , F?  ← 同一が 2 つ並んだら 2 ストライク

## 実験一覧

| ID | 定式化 | 仮説 | 主な変更 | ゲート基準 | OOF | LB | 失敗の層 | 状態 |
|---|---|---|---|---|---|---|---|---|
| exp001 | F1 | | | | | | 該当なし | DONE |

状態: `TODO` / `RUNNING` / `DONE` / `ABANDONED`
失敗の層: `該当なし`（合格） / `実装` / `ハイパラ` / `定式化`

**新しい実験を追加したら必ず 1 行追加する。**

## CV-LB 相関メモ

| 日付 | exp | OOF | Public LB | 差 (LB−OOF) | 備考 |
|---|---|---|---|---|---|

**週 1 回以上提出する（CLAUDE.md 第6節）。** 5-10 点取れたら OOF だけで意思決定できるようになる。

## 公開ベースライン目安

| Notebook | LB | 手法 | リークの有無 | URL |
|---|---|---|---|---|

→ 公開最高が ? 付近。**ただし公開 LB は train/test 重複などのショートカットで底上げされている
ことがある。実コードを読んでリークの有無を確認してから目標値の根拠にする。**

## ロードマップ

- [ ]

**計画の段数が `strategy.md` の「残り実験可能数」を超えていないか毎週確認する。**

## 失敗・廃案の記録

実験が失敗したり方針を変えたら、**ここに 1 行残してから削除**する。後で類似アイデアが
浮かんだときの無駄な再試行を避けるため。

| exp | 試したこと | 結果 | 廃案理由 | 失敗の層 |
|---|---|---|---|---|
